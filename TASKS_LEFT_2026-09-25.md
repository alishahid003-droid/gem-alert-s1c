# Tasks Left — Sept 25, 2026

1. [ ] Robinhood Chain — auto-buy (blocked: needs Uniswap v4 pool key data not yet fetched)
2. [ ] Robinhood Chain — auto-sell (same block)
3. [x] Dashboard — trade/fill history table (tx hash, filled amount, timestamp)
4. [x] Dashboard — realized P&L display (fixed the real bug behind it: sells never set filled_usd)
5. [x] Dashboard — closed-positions history table
6. [x] Dashboard — backend trade-log addition feeding items 3–5
7. [ ] Re-run Solana/RHC backtest once MadeOnSol's rate limit clears
8. [x] Fix 和平熊猫 duplicate-contract display-name scoring bug
9. [x] Speed — Layer 0b WebSocket client + worker built and wired into scheduler._handle_scored (commit 88bfc69, 10 mocked tests passing). NOT YET provably live: Mobula's Pulse Stream V2 is Growth/Enterprise-plan only (your key may be free tier) and BSC support on this endpoint is unconfirmed by Mobula's own docs -- run `python worker_pulse_websocket.py` once pushed to see the real accept/reject and the raw `init` message per chain.
10. [x] Real multi-coin backtest infra fixed (backtest.py). Along the way found and fixed a BIGGER, real production bug: Layer 1's `/deployer-hunter/alerts` calls (the live poll-fast.yml cron job, every 10 min) were sending `tier=elite,good` as one comma-joined param -- MadeOnSol's API rejects that with 400 and wants the tier repeated per-value instead. Confirmed live via diagnostic. This means Layer 1's elite/good deployer alerts had almost certainly NEVER actually come through in production, silently, since it was built (commit a04b144, regression test added). Also found MadeOnSol has NO rug/spam deployer tier at all (only elite/good/rising exist) -- backtest.py's methodology rewritten to compare elite/good vs. rising honestly instead of asserting a fabricated "should score C/D" hit rate (commit 05f107e). Real run of backtest.py then surfaced an even bigger finding (see #10b below): all 21 scored tokens came back with the IDENTICAL score (52) and band (B) -- 65% of every Solana score's weight was permanently defaulted, not real per-token data. Still needs you to run: `python backtest.py` again post-push for real, now-differentiated numbers.

10b. [x] Scoring-integrity fix: `holder_growth_rate_per_hr` (Solana AND BSC/Base) and Solana `vol_to_liq_ratio` were hardcoded to `None` throughout `layers/layer0_scoring.py`, despite comments falsely claiming "wired in scheduler" (confirmed false via grep -- zero references anywhere). That's 45pts of every Solana score's 100-point weight permanently defaulted to fixed neutral values, regardless of real per-token risk -- explains why 21 independent real tokens in the backtest all converged on the same score/band. Fixed with real, source-confirmed wiring: `state.py` gained a `record_holder_point`/`get_holder_history` per-token history (same pattern as the existing mc_history), a new `compute_holder_growth_rate_per_hr()` computes a real rate once 2+ points exist across a real time span, and a new `fetch_dexscreener_vol_liq()` pulls real volume/liquidity from DexScreener's public API (confirmed endpoint/fields via official docs -- MadeOnSol's own 3 endpoints carry no volume/liquidity figure at all). Solana holder-growth-rate deliberately left unwired -- no confirmed MadeOnSol field name exists for total holder count, only `top10_share`, and guessing one risks repeating the earlier "Mobula bug #5" field-name-guessing mistake. 290 tests passing (commit 6edd36a).
11. [x] MadeOnSol's remaining PRO-tier gap (LP-lock health + mint/freeze-authority revocation, the last 20pts of every Solana score) closed for free instead of a plan upgrade: extended `fetch_goplus_security()` to Solana via GoPlus's real, confirmed-live Solana token-security endpoint (real field names -- `mintable.status`/`freezable.status`/`lp_holders[].balance` -- confirmed via a diagnostic you ran from your own terminal, since the Cowork bridge itself can't reach gopluslabs.io). Only fires as a fallback when MadeOnSol's own `/risk` call fails (the real PRO-gate symptom), never overrides real MadeOnSol data. Commit `0f5dd35`, 297 tests passing. Every one of Layer 0's 100 Solana scoring points is now either real per-token data or an honestly-still-unwired gap (Solana holder-growth-rate only) -- no more silent fixed defaults anywhere in the formula.
12. [x] `entrypoint.py` already wired into `scheduler.py`'s live poll loop (confirmed, not new work — was already true, docstring was just stale)
13. [ ] Real test: 1 live Solana buy — needs you, real money
14. [ ] Real test: 1 live Solana sell — needs you, real money
15. [ ] Real test: 1 live BSC buy — needs you, real money
16. [ ] Real test: 1 live BSC sell — needs you, real money
17. [ ] Real test: 1 moonbag trim on a real live position — needs you, real money
18. [ ] Real test: dashboard checked against real trade data (now unblocked — item 6 is done, just needs 13-17 to produce real data to look at)
19. [ ] Keep `EXECUTION_ENABLED=false` until tasks 13-16 all pass
20. [ ] `git push origin main` — new commits ready since your last push, most recent first: 6edd36a (#10b holder-growth-rate/vol-liq real wiring, 290 tests), 05f107e (backtest.py tier-vocabulary methodology fix), 571a400/a04b144/109271a/35740be/a662046/2c53284 (#9 WebSocket work + #10 backtest fixes + the Layer 1 deployer-alerts production bug fix) — needs your GitHub login, my sandbox has no credentials for it
